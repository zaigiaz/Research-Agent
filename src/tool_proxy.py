import pydantic
import os

# TODO :: create counters with itertools for how many tool-calls for each agent / sub-agent
# TODO :: create checks for staying inside task_dir
# TODO :: create response messages back to AI or user in-case we run out or prematurely end task

